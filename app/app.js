var React = require('react');
var ReactDOM = require('react-dom');
var Container = require('./src/react-components/Container');

//$("#graph").toggleClass("graph");

ReactDOM.render(
	<Container />, document.getElementById('graph')
);
